import { ImageheightDirective } from './imageheight.directive';

describe('ImageheightDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageheightDirective();
    expect(directive).toBeTruthy();
  });
});
