import { TranslateImageDirective } from './translate-image.directive';

describe('TranslateImageDirective', () => {
  it('should create an instance', () => {
    const directive = new TranslateImageDirective();
    expect(directive).toBeTruthy();
  });
});
